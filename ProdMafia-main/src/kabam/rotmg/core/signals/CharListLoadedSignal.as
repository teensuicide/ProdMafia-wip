package kabam.rotmg.core.signals {
   import org.osflash.signals.Signal;
   
   public class CharListLoadedSignal extends Signal {
       
      
      public function CharListLoadedSignal() {
         super();
      }
   }
}
